{
  "name": "party",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "partykit dev",
    "build": "partykit build",
    "deploy": "partykit deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "partykit": "^0.0.115",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}

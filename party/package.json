{
  "name": "party",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "partykit dev",
    "build": "partykit build",
    "deploy": "partykit deploy"
  },
  "devDependencies": {
    "partykit": "^0.0.115",
    "typescript": "^5.6.0"
  }
}
